type: pipeline-template
version: 1

name: Java Maven CI (SCM + Cache)
displayName: Java CI – SCM Checkout & Maven Build

description: >
  This pipeline checks out source code from Git,
  restores the Maven dependency cache,
  runs a Maven clean build,
  and saves the cache for faster future builds.

parameters:
  - name: repoUrl
    type: string
    displayName: Git Repository URL
    description: URL of the Git repository
    required: true

  - name: branchName
    type: string
    displayName: Git Branch
    description: Branch to build
    defaultValue: main

  - name: gitCredentials
    type: credentials
    displayName: Git Credentials
    description: Credentials to access the Git repository
    required: true
